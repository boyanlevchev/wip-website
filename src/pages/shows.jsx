import React from 'react';

function Shows({ destination }) {

  return (
    <nav className={"page-padding flex w-100 "}>
      <section className={"text-column"}>
        <p className={"flex align-center"}>
          Shows will appear here soon.
        </p>
      </section>
    </nav>
  );
}

export default Shows;
